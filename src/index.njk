---
layout: layouts/base.njk
---
<div class="home-intro">
  <h1>食品工場からのキャリアアップを、一次情報で。</h1>
  <p>
    食品キャリアラボは、現役の食品メーカー社員が運営するキャリアメディアです。
    食品工場の現場から生産技術・品質保証・工場DXへ進む道筋と、
    食品業界で働く人の転職のリアルを、実体験に基づいて解説します。
  </p>
</div>

<h2>記事一覧</h2>
<ul class="article-list">
  {% for article in collections.articles %}
  <li>
    <time datetime="{{ article.date | isoDate }}">{{ article.date | readableDate }}</time><br>
    <a href="{{ article.url }}">{{ article.data.title }}</a>
    <p class="article-list-desc">{{ article.data.description }}</p>
  </li>
  {% endfor %}
</ul>
